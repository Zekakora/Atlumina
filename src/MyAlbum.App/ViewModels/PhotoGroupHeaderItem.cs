namespace MyAlbum_App.ViewModels;

/// <summary>A day / month / year group header row in the adaptive photo grid.</summary>
public sealed class PhotoGroupHeaderItem
{
    public required string Title { get; init; }
}
